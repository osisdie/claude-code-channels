# Credentials
.env
.env.*
!.env.example
**/.dev.vars

# Claude Code local settings (per-machine permissions/config)
.claude/settings.local.json

# Channel state (tokens, access control, inbox — per-channel)
.claude/channels/

# Playwright MCP screenshots
.playwright-mcp/

# Python
__pycache__/
*.pyc

# Node / Bun
node_modules/

# Cloudflare Workers
.wrangler/

# Official plugin backups (created by start.sh symlink)
*.official/

# Custom
firebase-debug.log
tmp/

# Output (yt2pdf, digests, etc.)
output/
